# Task kind: Review

You are working inside an automated pipeline. This task type is reserved for code review workflows (V0.2+); it is not wired into the V0.1 pipeline yet.

Reserved placeholder — the runner currently routes to `bug-fix` or `feature`.
